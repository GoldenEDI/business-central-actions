name: 'Install NavContainerHelper'
description: 'Installs the NavContainerHelper module.'
branding:
  icon: 'play'
  color: green
runs:
  using: composite

  steps:
    - id: install-navcontainerhelper
      shell: powershell
      run: ${{ github.action_path }}/setup.ps1